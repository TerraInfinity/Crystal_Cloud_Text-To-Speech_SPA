# Python Backend System Requirements (gTTS server, optional)

- Python 3.x
- ffmpeg (must be installed and available in your system PATH)
- pip install -r requirements.txt  to install 

# Frontend 
- npm run install